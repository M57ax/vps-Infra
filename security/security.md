# VPS Hardening

## Firewall (UFW)

- allow OpenSSH(22)
- deny all incoming by default

## Fail2ban

- enabled
- sshd jail active

still some errors. hostinger terminal doesnt work but ssh connections works normal.
Have to figure out why this is happening.
